# photo-date-sorter

按拍摄日期整理 Nikon（及其他）相机照片/视频：支持一键导卡、EXIF 日期、重复导入去重、冗余 `_1/_2` 安全清理（导出 CSV 后确认删除）等。

运行：安装 `requirements.txt` 后执行 `python photo_date_sorter.py`，或使用 PyInstaller 自行打包。

详见仓库内 `photo_date_sorter.py` 顶部说明与日志。
